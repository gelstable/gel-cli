# Load the Nix devshell for this repo if Nix is available.
if has nix; then
  use flake || echo "direnv: Note: 'use flake' failed. Ensure nix-direnv is installed and Nix flakes are enabled." >&2
else
  echo "direnv: Nix is not installed; skipping devshell setup." >&2
fi
