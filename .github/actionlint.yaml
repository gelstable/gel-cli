# actionlint does not yet recognize GitHub's standard windows-11-arm preview label.
# Registering it here affects linting only; the workflow still uses a GitHub-hosted runner.
self-hosted-runner:
  labels:
    - windows-11-arm
